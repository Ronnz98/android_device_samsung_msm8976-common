allow rild proc_net:file w_file_perms;

r_dir_file(rild, app_efs_file)
allow rild efs_file:dir r_dir_perms;
allow rild efs_file:file r_file_perms;

allow rild rild_socket:sock_file { create setattr unlink };

set_prop(rild, system_radio_prop)
set_prop(rild, radio_prop)

allow rild self:tun_socket create_socket_perms_no_ioctl;

allow rild sysfs_net:dir search;
allow rild sysfs_net:file r_file_perms;

allow rild biometrics_vendor_data_file:dir search;

